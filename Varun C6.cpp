//Program inorder to add three numbers a, b and c
#include <stdio.h>

int main() {
    int a, b, c;
    
    printf("Enter three numbers you want to add: ");
    scanf("%d %d %d", &a, &b, &c);
    
    int sum = a + b + c;
    
    printf("a=%d, b=%d, c=%d, sum=%d\n", a, b, c, sum);
    
    return 0;
} 
//chatgpt corrected one. 

